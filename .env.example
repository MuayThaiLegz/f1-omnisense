# F1 OmniSense — Environment Variables
# Copy to .env and fill in your values: cp .env.example .env

# ── MongoDB Atlas ──────────────────────────────────────
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/
MONGODB_DB=McLaren_f1

# ── Groq API (required) ───────────────────────────────
GROQ_API_KEY=gsk_...
GROQ_REASONING_MODEL=llama-3.3-70b-versatile
GROQ_QUICK_MODEL=llama-3.1-8b-instant

# ── 3D Generation APIs (optional) ─────────────────────
MESHY_API_KEY=
TRIPO_API_KEY=
FAL_KEY=

# ── LLM Fallbacks (optional) ──────────────────────────
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-6

# ── Server Config ─────────────────────────────────────
API_PORT=8100
PRIMARY_LLM_PROVIDER=groq
DEBUG=false
LOG_LEVEL=INFO
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80

# ── HuggingFace (sentence-transformers model cache) ───
# Optional: set to cache models outside container
# HF_HOME=/app/.cache/huggingface

# ── Volume path overrides (optional) ──────────────────
# Defaults to project subdirectories
# F1_DATA_PATH=/absolute/path/to/f1data
# PIPELINE_OUTPUT_PATH=/absolute/path/to/pipeline/output
# DATA_PATH=/absolute/path/to/data
# GLB_MODELS_PATH=/absolute/path/to/frontend/public/models
